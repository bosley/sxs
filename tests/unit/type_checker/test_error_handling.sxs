[
  (def test-assert (fn (x :int) :int [
    (assert 1 "always passes")
    42
  ]))

  (def safe-div (fn (a :int b :int) :int [
    (recover
      [10]
      [0])
  ]))

  (def try-op (fn (val :int) :int [
    (try 42 0)
  ]))
]

