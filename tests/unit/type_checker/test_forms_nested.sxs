[
  #(define-form coords {:int :int})
  #(define-form rect {:coords :coords})
  #(define-form box {:rect :str})
  
  (def top_left (cast :coords {0 0}))
  (def bottom_right (cast :coords {100 100}))
  (def rectangle (cast :rect {top_left bottom_right}))
  (def labeled_box (cast :box {rectangle "MyBox"}))
  
  (debug labeled_box)
]

