[
  (def result-true (if 1 42 99))
  (def result-false (if 0 42 99))
  
  (def result-nonzero (if 5 100 200))
  (def result-negative (if -1 100 200))
  
  (def result-non-int-symbol (if some-symbol 111 222))
  (def result-non-int-real (if 3.14 111 222))
  
  (def x 10)
  (def result-with-eval (if x 333 444))
  
  (def result-with-bracket-true (if 1 [
    (def a 1)
    (def b 2)
    (def sum 3)
  ] [
    (def c 4)
  ]))
  
  (def result-with-bracket-false (if 0 [
    (def d 5)
  ] [
    (def e 6)
    (def f 7)
    (def product 42)
  ]))
  
  (def check-fn (fn (val :int) :int [
    (if val 777 888)
  ]))
  
  (def fn-result-true (check-fn 1))
  (def fn-result-false (check-fn 0))
]

