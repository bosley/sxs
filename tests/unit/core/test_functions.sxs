[
    (def add-two (fn (a :int b :int) :int [
        (debug "Inside add-two:" a b)
        (def result-placeholder 0)
    ]))
    
    (debug "Function defined, type check")
    
    (def simple-fn (fn () :int [
        (debug "No params function")
        (def ret 42)
    ]))
    
    (debug "Calling simple-fn")
    (simple-fn)
    
    (debug "Calling add-two with args")
    (add-two 10 20)
    
    (def with-return (fn (x :int) :int [
        (debug "Param value:" x)
        (def doubled 999)
    ]))
    
    (with-return 5)
    
    (def multi-param (fn (a :int b :real c :symbol) :int [
        (debug "Multi params:" a b c)
        (def done 1)
    ]))
    
    (multi-param 100 3.14 test-symbol)
]

