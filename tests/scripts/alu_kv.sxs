[
    #(load "alu" "kv" "io")

    (kv/open-memory mem)

    (kv/set mem:x 1)
    (kv/set mem:y 2)
    (kv/set mem:z 3)

    (def x (kv/get mem:x))
    (def y (kv/get mem:y))
    (def z (kv/get mem:z))

    (io/put "x: %d y: %d z: %d\n" x y z)

]