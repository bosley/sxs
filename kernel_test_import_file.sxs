[
    ; This is valid ad the file doesn't know if its going to be imported.
    ; the way the import should work is only allow kernel loads at the top
    ; along with file imports. with this being enforced as the first thing to do
    ; we can guard against already imported items (its success)
    ; then, from root file, when done loading and importing, we disable the
    ; ability to import anything else for the sake of a stable runtime
    ; if we want to reload or swap later, we can add new logic. 
    #(load "io")

    (export some_function (fn (argument :int) :any [
        (io/put "You called me with! %d" argument)
    ]))
]