[
  (def add (fn (a :int b :int) :int [
    42
  ]))

  (def multiply (fn (x :int y :int) :int [
    100
  ]))

  (def to-string (fn (val :int) :str [
    "converted"
  ]))

  (def identity (fn (x :int) :int [
    0
  ]))
]

