[
    (export add (fn (a :int b :int) :int [
        (debug "Adding" a "and" b)
        (def result 8)
        result
    ]))
    
    (export multiply (fn (a :int b :int) :int [
        (debug "Multiplying" a "and" b)
        (def result 30)
        result
    ]))
]

