[
  (def immediate-exit (do [
    (done 42)
  ]))

  (def return-string (do [
    (done "hello")
  ]))

  (def check-iterations (do [
    (done $iterations)
  ]))

  (def nested-outer (do [
    (def inner-result (do [
      (done 100)
    ]))
    (done inner-result)
  ]))

  (def loop-fn (fn () :int [
    (do [
      (done 999)
    ])
  ]))

  (def loop-result-from-fn (loop-fn))

  (def with-bracket-body (do [
    (def temp 1)
    (def temp2 2)
    (done 3)
  ]))
]

