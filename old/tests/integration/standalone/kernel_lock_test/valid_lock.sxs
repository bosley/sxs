[
    #(load "io")
    #(load "alu")
    
    (io/put "Kernels loaded successfully before any code execution\n")
    (def result (alu/add 10 20))
    (io/put "10 + 20 = %d\n" result)
    (io/put "Test passed: kernels can be loaded in datum expressions\n")
]

