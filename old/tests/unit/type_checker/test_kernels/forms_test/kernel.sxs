[
#(define-form pair {:int :int})
#(define-form triple {:int :int :int})
#(define-form result {:str :int :error})
#(define-form point {:real :real})
#(define-form nested {:pair :str})
#(define-kernel forms_test "libkernel_forms_test.dylib" [
    (define-function make_pair (a :int b :int) :pair)
    (define-function sum_pair (p :pair) :int)
    (define-function make_result (msg :str code :int err :error) :result)
    (define-function process_batch (items :pair..) :result)
    (define-function make_point (x :real y :real) :point)
    (define-function distance (p1 :point p2 :point) :real)
])
]

