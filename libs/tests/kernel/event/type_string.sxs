[
    #(load "event" "io")

    (io/put "=== String Type Test ===\n")

    (event/init)

    (event/subscribe strTopic (fn (evt :any) :any [
        (io/put "Got string: %s\n" evt)
    ]))

    (event/publish strTopic "hello")
    (event/publish strTopic "world")
    (event/publish strTopic "")
    (event/publish strTopic "test message")

    (io/put "Published 4 strings\n")

    (io/put "\n=== Test complete ===\n")
]
