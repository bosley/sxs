[
    #(load "event" "io")

    (io/put "=== Symbol Type Test ===\n")

    (event/subscribe symTopic (fn (evt :any) :any [
        (io/put "Got symbol event\n")
    ]))

    (event/publish symTopic 'mySymbol)
    (event/publish symTopic 'anotherSymbol)
    (event/publish symTopic 'x)

    (io/put "Published 3 symbols\n")

    (io/put "\n=== Test complete ===\n")
]
