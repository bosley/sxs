# Steps 

Make ~/.sxs 

inside ~/.sxs make a "libs" dir that installs the core libraries and slp libraries to theire respective install dirs
so the application can find them there. We will build against this install as the runtime will also use this install
and shared libraries we produce

